# symmetrical-stream

User logs on and is automatically connected to the signaling server. 

TUI: 

User selects a peer to connect with. 
- Offer is generated and sent to the peer
- Peer generates an answer and sends it back
    + need to make this something a user initiates rather than automatic
- Both sides complete the handshake

